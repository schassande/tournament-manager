/**
 * Re-export the shared persistent data model so the Functions build can compile
 * it directly from source without depending on a separate npm package build.
 */
export * from '../../persistent-data-model/src';
